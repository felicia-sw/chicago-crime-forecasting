# Large raw data (exceeds GitHub's 100 MB limit — keep local only)
Crimes_-_2001_to_Present.csv
*.csv.gz
*.parquet

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# Environment / secrets
.env
.env.*

# OS / editor
.DS_Store
.vscode/
.idea/

# Temp / build
*.log
*.tmp
build/
dist/

# Intermediate per-district model checkpoints (regenerable; superseded by base_forecasts.csv)
# (matches main run + any alt-config run, e.g. regime2020/)
**/checkpoints/

# Throwaway smoke/canary/test backtest outputs (not real results), in any run dir
base_forecasts_smoke*.csv
base_forecasts_canary.csv
base_forecasts_lighttest.csv
base_forecasts_partial.csv

# Claude Code tooling — kept local only, not part of the published repo
.claude/
CLAUDE.md
manuscript/*.docx
